<?xml version="1.0" encoding="utf-8"?>
<!-- Nothing is backed up or transferred off the device. The worker's pay data
     stays on the phone; the in-app 백업 BACKUP export is the only way out, and
     the worker chooses where it goes. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
